---
import { Picture } from "astro:assets";
import heroImage from "@/assets/hero.png";
import Link from "@/components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 flex justify-center items-center w-full">
  <!-- Collage de fotos reales de eventos -->
  <div class="relative flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8 fadein-collage">
    <!-- Imagen secundaria izquierda -->
    <img
      src={`${import.meta.env.BASE_URL}punto-saas-3-meetup.jpeg`}
      alt="Evento Punto SaaS 3"
      class="w-28 h-28 md:w-40 md:h-40 object-cover shadow-lg collage-img-secondary"
      style="border-radius: 38% 62% 63% 37% / 48% 38% 62% 52%;"
      loading="lazy"
    />
    <!-- Imagen principal -->
    <div class="relative">
      <img
        src={`${import.meta.env.BASE_URL}punto-saas-2-meetup.jpeg`}
        alt="Evento Punto SaaS 2 (principal)"
        class="w-44 h-44 md:w-64 md:h-64 object-cover shadow-2xl collage-img-main"
        style="border-radius: 41% 59% 70% 30% / 38% 65% 35% 62%;"
        loading="eager"
      />
      <!-- Badge -->
      <div class="absolute bottom-2 right-2 flex items-center px-3 py-1 rounded-full text-xs font-medium badge-desayuno"
        style="background: rgba(255,255,255,0.9); color: #222; border: 1px solid #e5e7eb; box-shadow: 0 1px 6px 0 rgba(0,0,0,0.08);">
        🥐 Así lucen nuestros desayunos curados
      </div>
    </div>
    <!-- Imagen secundaria derecha -->
    <img
      src={`${import.meta.env.BASE_URL}punto-saas-1-meetup.jpeg`}
      alt="Evento Punto SaaS 1"
      class="w-28 h-28 md:w-40 md:h-40 object-cover shadow-lg collage-img-secondary"
      style="border-radius: 58% 42% 43% 57% / 41% 53% 47% 59%;"
      loading="lazy"
    />
  </div>
  <style>
    .fadein-collage > * {
      opacity: 0;
      transform: scale(0.96);
      animation: collage-fadein 0.85s cubic-bezier(.4,1.3,.6,1) forwards;
    }
    .fadein-collage > *:nth-child(1) { animation-delay: 0.1s; }
    .fadein-collage > *:nth-child(2) { animation-delay: 0.2s; }
    .fadein-collage > *:nth-child(3) { animation-delay: 0.3s; }
    @keyframes collage-fadein {
      to { opacity: 1; transform: scale(1); }
    }
    @media (max-width: 768px) {
      .fadein-collage {
        flex-direction: column !important;
      }
      .collage-img-main { width: 10rem !important; height: 10rem !important; }
      .collage-img-secondary { width: 6rem !important; height: 6rem !important; }
    }
  </style>
</div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      Conecta con decisores en Perú
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      Impulsa las ventas de tu startup B2B SaaS reduciendo tu CAC. Participa en desayunos curados que te conectan directamente con empresas medianas y grandes listas para comprar.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#"
        href="https://web3templates.com/templates/astroship-starter-website-template-for-astro"
        target="_blank"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxs-cloud-download" />

        Aplicar
      </Link>
      <Link
        size="lg"
        style="outline"
        rel="noopener"
        href="https://github.com/surjithctly/astroship"
        class="flex gap-1 items-center justify-center"
        target="_blank">
        <Icon class="text-black w-4 h-4" name="bx:bxl-github" />
        Más Información
      </Link>
    </div>
  </div>

