---
// @ts-ignore
import { Icon } from "astro-icon/components";

const features = [
  {
    title: "Conexiones Reales",
    description:
      "Conectamos a startups con sus clientes ideales en un entorno de confianza. Esto permite obtener feedback valioso y validar ideas rápidamente.",
    icon: "bx:bxs-briefcase",
  },
  {
    title: "Ahorra Tiempo",
    description:
      "Reduce tu costo de adquisición de clientes con introducciones directas. Esto acelera tu proceso de ventas y te acerca al cierre más rápido.",
    icon: "bx:bxs-data",
  },
  {
    title: "Impacto Real",
    description:
      "Diseñamos cada encuentro para que los fundadores obtengan oportunidades tangibles. Aquí no hay espacio para la teoría, solo resultados.",
    icon: "bx:bxs-bot",
  },
  {
    title: "Crecimiento Acelerado",
    description:
      "En solo 60 minutos, podrás validar tus ideas con feedback directo. Aprovecha esta oportunidad para transformar tu enfoque comercial.",
    icon: "bx:bxs-file-find",
  },
];
---

<section class="py-20 fadein-section bg-white">
  <div class="container mx-auto p-4 md:p-6 lg:p-8">
    <div class="flex flex-col items-center text-center mb-14">
      <span class="block mx-auto text-center max-w-max uppercase text-xs font-semibold tracking-widest mb-3 px-3 py-1 rounded-full bg-[var(--color-primary)]/10 text-[var(--color-primary)]">POR QUÉ NOSOTROS</span>
      <h2 class="text-4xl md:text-5xl font-bold mb-4 text-[var(--color-text)]">Diferenciadores Clave de PuntoSaaS</h2>
      <p class="text-lg text-[var(--color-text)]/70 max-w-2xl mx-auto">
        PuntoSaaS te ofrece la oportunidad de tener conversaciones significativas con decisores clave. Sin presentaciones, solo un diálogo auténtico que impulsa resultados.
      </p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {features.map((item) => (
        <div class="bg-white border border-[var(--color-border)] rounded-xl shadow-sm p-8 flex flex-col items-start h-full">
          <div class="mb-5 flex items-center justify-center rounded-lg bg-[var(--color-primary)]/10 w-11 h-11">
            <Icon class="text-[var(--color-primary)] w-6 h-6" name={item.icon} />
          </div>
          <h3 class="text-3xl font-bold text-center text-[var(--color-text)] mb-3">{item.title}</h3>
          <p class="text-[var(--color-text)]/70 leading-relaxed">{item.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
